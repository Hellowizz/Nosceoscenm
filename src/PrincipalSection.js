import React from 'react';
import PropTypes from "prop-types";

export default class PrincipalSection extends React.Component {
  static propTypes = {
    /*name: PropTypes.string,
    description: PropTypes.string,
    image: PropTypes.any*/
  };

  render()  {
  	<div className="principal-container">
  	</div>
  }

}